
{{!-- find a way to make a recipe a fav in the db. Then display fav recipes here --}}
{{> recipe-info}}

<section>
    
  <h2>Create New Post</h2>

  <form class="new-post-form">
    <div>
      <label for="post-title">Title</label>
      <input type="text" id="post-title" name="post-title" />
    </div>
    <div>
      <label for="post-url">Link</label>
      <input id="post-url" name="post-url" />
    </div>
    <button type="submit" class="btn">Create</button>
  </form>

</section>
<div>hello1</div>
 {{!-- {{log recipe}} --}}
 {{!-- {{#if recipe}} --}}
 {{recipe}}
 {{log recipe.recipe.label}}

    {{#each recipe as |recipes|}}
<div>hello2</div>
    {{!-- {{log recipes.recipe.image}} --}}
    <div>
    {{log recipes.recipe.label}}
title

    {{recipes.recipe.label}}
      <img src="{{recipes.recipe.image}}" alt="picture of food">
    </div>
    {{/each}}
    {{!-- {{/if}} --}}
{{!-- {{#if posts.length}}
<section>
  <h2>Your Posts</h2>
  <ol>
    {{#each posts as |post|}}
    <li>
      {{> post-info post}}
      <a href="/dashboard/edit/{{post.id}}" class="edit-link">Edit post</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}} --}}

<script src="/javascript/add-post.js"></script>
{{!-- <script src="/javascript/recipes.js"></script> --}}
